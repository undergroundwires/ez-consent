/**
 * @see https://prettier.io/docs/en/configuration.html
 * @type {import("prettier").Config}
 */
const config = {
  tabWidth: 2, // Or it conflicts with `markdownlint`
  singleQuote: true,
};

export default config;
